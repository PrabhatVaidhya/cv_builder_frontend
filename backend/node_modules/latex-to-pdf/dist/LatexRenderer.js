"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LatexRenderer = void 0;
const fs = __importStar(require("fs"));
const handlebars_1 = __importDefault(require("handlebars"));
const TemplateParser_1 = require("./TemplateParser");
handlebars_1.default.registerHelper("sub", function (a, b) {
    return a - b;
});
handlebars_1.default.registerHelper("add", function (a, b) {
    return a + b;
});
handlebars_1.default.registerHelper("gt", function (a, b) {
    return a > b;
});
handlebars_1.default.registerHelper("lt", function (a, b) {
    return a < b;
});
handlebars_1.default.registerHelper("eq", function (a, b) {
    return a === b;
});
handlebars_1.default.registerHelper("and", function (a, b) {
    return a && b;
});
handlebars_1.default.registerHelper("or", function (a, b) {
    return a || b;
});
handlebars_1.default.registerHelper("not", function (a) {
    return !a;
});
handlebars_1.default.registerHelper("innerFieldNames", function (array) {
    if (!array) {
        console.error("Array is undefined. Cannot get inner keys.");
        console.error(array);
        return [];
    }
    let field_names = [];
    for (let i = 0; i < array.length; i++) {
        if (array[i]["field_name"]) {
            field_names.push(array[i]["field_name"]);
        }
    }
    return field_names;
});
handlebars_1.default.registerHelper("innerFieldDescriptions", function (obj) {
});
// Register a "keys" helper to get the keys of an object
handlebars_1.default.registerHelper("keys", function (obj) {
    if (!obj) {
        console.error("Object is undefined. Cannot get keys.");
        console.error(obj);
        return [];
    }
    return Object.keys(obj);
});
// Register a "lookup" helper to retrieve a value from an object by key
handlebars_1.default.registerHelper("lookup", function (obj, key) {
    if (!obj) {
        console.error("Object is undefined. Cannot lookup key.");
        console.error(obj);
        return undefined;
    }
    return obj[key];
});
handlebars_1.default.registerHelper("latexClean", function (text) {
    return text
        .replace(/&#x27;/g, "'") // Escape backslashes
        .replace(/&/g, "\\&")
        .replace(/%/g, "\\%")
        .replace(/#/g, "\\#")
        .replace(/_/g, "\\_");
});
handlebars_1.default.registerHelper("snakeToPascalSpace", function (text) {
    return text
        .replace(/_/g, " ")
        .replace(/\b\w/g, function (char) {
        return char.toUpperCase();
    });
});
handlebars_1.default.registerHelper("snakeToTildeSpace", function (text) {
    return text
        .replace(/_/g, "~")
        .replace(/\b\w/g, function (char) {
        return char.toUpperCase();
    });
});
handlebars_1.default.registerHelper("enumerate", function (array) {
    return array.map((item, index) => {
        return { index: index, value: item };
    });
});
handlebars_1.default.registerHelper("labSheetCompoundSpaces", function (array) {
    if (!array) {
        console.error("No compounds in the lab sheet.");
        return "";
    }
    if (array.length > 24) {
        console.error("Too many compounds in the lab sheet. Maximum is 24.");
    }
    ;
    let cells = array.map((item, index) => {
        return "\\scriptsize " + item;
    });
    while (cells.length < 24) {
        cells.push(" ");
    }
    let row1 = cells.slice(0, 12).join(" & ");
    let row2 = cells.slice(12, 24).join(" & ");
    return new handlebars_1.default.SafeString(row1 + " \\\\[0.66cm] \\hline " + row2 + " \\\\[0.66cm] \\hline ");
});
handlebars_1.default.registerHelper("trialLabSheetCompoundSpaces", function (array) {
    if (!array) {
        console.error("No compounds in the lab sheet.");
        return "";
    }
    if (array.length > 8) {
        console.error("Too many compounds in the lab sheet. Maximum is 8.");
    }
    ;
    let cells = array.map((item, index) => {
        return "\\scriptsize " + item;
    });
    while (cells.length < 8) {
        cells.push(" ");
    }
    let row1 = cells.slice(0, 4).join(" & ");
    let row2 = cells.slice(4, 8).join(" & ");
    return new handlebars_1.default.SafeString(row1 + " \\\\[0.66cm] \\hline " + row2 + " \\\\[0.66cm] \\hline ");
});
class LatexRenderer {
    static populateTemplate(latexFilePath, interface_data, output) {
        let templateContent = fs.readFileSync(latexFilePath, "utf-8");
        // Remove bindings
        const regex = TemplateParser_1.bindingRegex.binding;
        templateContent = templateContent.replace(regex, "");
        const template = handlebars_1.default.compile(templateContent);
        const renderedLatex = template(interface_data);
        const outputFilePath = output.replace(".pdf", ".tex");
        fs.writeFileSync(outputFilePath, renderedLatex);
        return outputFilePath;
    }
}
exports.LatexRenderer = LatexRenderer;
