import { StdioOptions } from 'child_process';
export declare class LatexToPdf {
    private templatePath;
    private static reportTypeToPathMap;
    constructor(reportType: string);
    populate<T>(data: T, output: string, stdioOption?: StdioOptions): string;
    private static loadReportTypeMap;
}
export declare function generateAllTypes(templateDir: string): string[];
