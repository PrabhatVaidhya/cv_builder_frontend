"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LatexToPdf = void 0;
exports.generateAllTypes = generateAllTypes;
const TemplateParser_1 = require("./TemplateParser");
const TypeGenerator_1 = require("./TypeGenerator");
const LatexRenderer_1 = require("./LatexRenderer");
const PdfGenerator_1 = require("./PdfGenerator");
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
// Define ReportType as a union of string literals
const REPORT_MAP_FILE = path_1.default.join(__dirname, 'reportTypeMap.json');
class LatexToPdf {
    constructor(reportType) {
        var _a;
        if (!LatexToPdf.reportTypeToPathMap) {
            LatexToPdf.loadReportTypeMap();
        }
        const templatePath = (_a = LatexToPdf.reportTypeToPathMap) === null || _a === void 0 ? void 0 : _a[reportType];
        if (!templatePath) {
            throw new Error(`Template path not found for report type: ${reportType}\nValid types: ${Object.keys(LatexToPdf.reportTypeToPathMap || {}).join(', ')}`);
        }
        this.templatePath = templatePath;
        let parsed = TemplateParser_1.TemplateParser.extractFields(this.templatePath);
        //let interfaceName = TypeGenerator.writeInterface(templatePath,parsed);
        //console.log(`Generated TypeScript interface: ${interfaceName}`);
    }
    populate(data, output, stdioOption = 'ignore') {
        const populatedTex = LatexRenderer_1.LatexRenderer.populateTemplate(this.templatePath, data, output);
        return PdfGenerator_1.PdfGenerator.generatePdf(populatedTex, output, stdioOption);
    }
    static loadReportTypeMap() {
        if (fs_1.default.existsSync(REPORT_MAP_FILE)) {
            LatexToPdf.reportTypeToPathMap = JSON.parse(fs_1.default.readFileSync(REPORT_MAP_FILE, 'utf-8'));
        }
        else {
            throw new Error("Report type mapping file not found. Run `generateAllTypes` first.");
        }
    }
}
exports.LatexToPdf = LatexToPdf;
LatexToPdf.reportTypeToPathMap = null;
function generateAllTypes(templateDir) {
    const templateFiles = fs_1.default.readdirSync(templateDir).filter(file => file.endsWith('.tex') || file.endsWith('.tytex'));
    const reportTypeToPathMap = {};
    const reportTypes = templateFiles.map(templateFile => {
        const reportName = path_1.default.basename(templateFile, '.tex').replace(/-/g, '_');
        const templatePath = path_1.default.join(templateDir, templateFile);
        reportTypeToPathMap[reportName] = templatePath;
        let parsed = TemplateParser_1.TemplateParser.extractFields(templatePath);
        let interfaceName = TypeGenerator_1.TypeGenerator.writeInterface(templatePath, parsed);
        return reportName;
    });
    // Write the mapping to a JSON file
    fs_1.default.writeFileSync(REPORT_MAP_FILE, JSON.stringify(reportTypeToPathMap, null, 2));
    return reportTypes;
}
